{
  "name": "docker-agent",
  "version": "1.0.0",
  "description": "Reports Docker container status to homelab-dashboard (register + discover + report)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
